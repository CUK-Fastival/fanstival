<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fanstival.mapper.BoardMapper">

    <resultMap id="boardResultMap" type="fanstival.domain.Board">
        <id property="board_id" column="board_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="regdate" column="regdate"/>
        <result property="updatedate" column="updatedate"/>
        <result property="deletedate" column="deletedate"/>
    </resultMap>

    <select id="getBoards" resultMap="boardResultMap">
        SELECT *
        FROM board
        WHERE deletedate IS NULL
        ORDER BY regdate DESC
    </select>

    <select id="getBoardById" resultMap="boardResultMap" parameterType="string">
        SELECT *
        FROM board
        WHERE board_id = #{board_id} AND deletedate IS NULL
    </select>

    <insert id="createBoard" parameterType="fanstival.domain.Board">
        INSERT INTO board(board_id, title, content, regdate)
        VALUES (#{board_id}, #{title}, #{content}, #{regdate})
    </insert>

    <update id="updateBoard" parameterType="fanstival.domain.Board">
        UPDATE board
        SET title = #{title},
            content = #{content},
            updatedate = #{updatedate}
        WHERE board_id = #{board_id} AND deletedate IS NULL
    </update>

    <update id="deleteBoard" parameterType="string">
        UPDATE board
        SET deletedate = #{deletedate}
        WHERE board_id = #{board_id} AND deletedate IS NULL
    </update>

</mapper>
